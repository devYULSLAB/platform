# Generated by AI assistant
# Server port configuration for platform10
server:
  port: 9005

# Spring MVC and Thymeleaf settings
spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true # for using _method=PUT/DELETE in forms
  thymeleaf:
    cache: false # disable cache for development

# Application switcher URLs (as described in project-archtecture.txt)
# These would be the URLs for the other applications in the monorepo
app-switcher:
  urls:
    cmms: http://localhost:9010
    wflow: http://localhost:9020
    market: http://localhost:9030
    mportal: http://localhost:9040
